services:
  - type: web
    name: tele-bot-file1go
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PING_URL
        value: https://tele-bot-file1go.onrender.com

